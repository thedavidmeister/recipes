# pr-screenshots

Orphan branch hosting PR screenshots for this repo (GitHub has no API to upload
images into a comment, so they live on a branch and are embedded by raw URL).

`shots/<pr>.png`, or `shots/<pr>-<label>.png` when a PR needs more than one.
