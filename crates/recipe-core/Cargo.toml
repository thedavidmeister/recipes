[package]
name = "recipe-core"
version = "0.1.0"
edition = "2021"
description = "Shared recipe normalization (schema.org + TheMealDB) — native + wasm"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tl = "0.7"
url = "2"
