// Fully client-rendered SPA: no SSR, no prerender. The browser renders, drives
// ingestion and reads Turso directly; it parses nothing (see README).
export const ssr = false;
export const prerender = false;
