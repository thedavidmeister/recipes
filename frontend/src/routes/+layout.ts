// Fully client-rendered SPA: no SSR, no prerender. All logic runs in the
// browser (recipe-wasm parsing, direct Turso reads, TheMealDB fetches).
export const ssr = false;
export const prerender = false;
