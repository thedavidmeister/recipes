# Backend base URL (the fetch-proxy + write-gateway). Local dev default below.
PUBLIC_BACKEND_URL=http://localhost:8080

# Turso — the read-only token + URL for direct browser reads of the corpus.
# (These are PUBLIC: the read-only token ships to the browser by design.)
PUBLIC_TURSO_URL=libsql://recipes-thedavidmeister.aws-eu-west-1.turso.io
PUBLIC_TURSO_TOKEN=
